class Solution(object):
    def smallestRange(self, nums):
        """
        :type nums: List[List[int]]
        :rtype: List[int]
        """

        # sort all list into one list
        lenth = len(nums)
        sortedlst = []
        queuenum = [0] * lenth
        # TODO
        for queue_index, alst in enumerate(nums):
            pass
